<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ab.model">
    <select id="123a" parameterType="Integer" resultType="com.ab.model.Getphone">
        select * from getUserNameByPhone
        where id = #{id};
    </select>

    <select id="select123" parameterType="java.lang.String" resultType="map">
    #{sql};
    </select>
</mapper>